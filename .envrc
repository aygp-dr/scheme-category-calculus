#!/usr/bin/env bash
# Project environment configuration

export PROJECT_NAME="scheme-category-calculus"
export PROJECT_ROOT="$(pwd)"

# Guile configuration
export GUILE_LOAD_PATH="${PROJECT_ROOT}/src:${GUILE_LOAD_PATH}"
export GUILE_LOAD_COMPILED_PATH="${PROJECT_ROOT}/.cache:${GUILE_LOAD_COMPILED_PATH}"

# Development environment
export EDITOR="${EDITOR:-emacs}"
export TMUX_SESSION="${PROJECT_NAME}"